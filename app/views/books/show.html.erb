<main>
  <div>
      <%= flash[:notice] %>
  </div>

  <div class="book-comment">
    <h6>Title:</h6>
    <p><%= @book.title %></p>
    <h6>Body:</h6>
    <p><%= @book.body %></p>
  </div>
  
  <%= link_to "Edit", edit_book_path(@book.id) %>
  <%= link_to "Back", '/books' %>
</main>